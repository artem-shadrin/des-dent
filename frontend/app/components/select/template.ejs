<%
const cfg = { ...require("./data.json"), ...arguments?.[0] }
const {
	extraClasses,
	extraAttrs
} = cfg
%>

<%
const classes = `select ${extraClasses.join(" ")}`.trim()
const attrs = extraAttrs.join(" ")
%>

<select
	class="<%= classes %>"
	<%- attrs %>
>

</select>
